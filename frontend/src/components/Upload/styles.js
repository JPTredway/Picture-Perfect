import styled from "styled-components";

const StyledUpload = styled.div`
  margin-top: 3rem;
  display: flex;
  justify-content: center;
`;

const StyledImg = styled.img`
  display: block;
  margin: 0 auto;
  width: 100%;
  height: auto;
`;

export { StyledUpload, StyledImg };
